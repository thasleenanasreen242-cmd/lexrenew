'use client';
import {useEffect,useMemo,useState} from 'react';
import {Bell,CalendarClock,CheckCircle2,ChevronRight,Clock3,FileText,LayoutDashboard,Plus,Search,ShieldCheck,TriangleAlert,Users,Sparkles,CalendarDays,ClipboardList} from 'lucide-react';
import Link from 'next/link';

type Item={name:string;type:string;counterparty:string;expiry:string;days:number;status:'Urgent'|'Upcoming'|'Healthy'};
const initial:Item[]=[
{name:'Commercial Lease Agreement',type:'Lease',counterparty:'Harbor Properties LLC',expiry:'18 Sep 2026',days:6,status:'Urgent'},
{name:'Professional Indemnity Insurance',type:'Insurance',counterparty:'ABC Insurance',expiry:'27 Sep 2026',days:15,status:'Urgent'},
{name:'Annual Maintenance Contract',type:'AMC',counterparty:'FlowTech Services',expiry:'12 Oct 2026',days:30,status:'Upcoming'},
{name:'Trade Licence',type:'Licence',counterparty:'Department of Commerce',expiry:'08 Nov 2026',days:57,status:'Upcoming'},
{name:'Supplier Framework Agreement',type:'Contract',counterparty:'Vertex Industrial',expiry:'16 Dec 2026',days:95,status:'Healthy'},
];
export default function Home(){const [items,setItems]=useState<Item[]>(initial);const [q,setQ]=useState('');const [filter,setFilter]=useState('All');const [modal,setModal]=useState(false);const [toast,setToast]=useState('');
useEffect(()=>{try{const saved=localStorage.getItem('lexrenew-obligations');if(saved)setItems(JSON.parse(saved));}catch{}} ,[]);
useEffect(()=>{try{localStorage.setItem('lexrenew-obligations',JSON.stringify(items));}catch{}} ,[items]);
useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),2500);return()=>clearTimeout(t)},[toast]);
const filtered=useMemo(()=>items.filter(x=>(filter==='All'||x.type===filter)&&`${x.name} ${x.counterparty}`.toLowerCase().includes(q.toLowerCase())),[items,q,filter]);
const add=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const expiry=String(f.get('expiry'));const d=new Date(expiry);const days=Math.max(0,Math.ceil((d.getTime()-Date.now())/86400000));const status:Item['status']=days<=30?'Urgent':days<=60?'Upcoming':'Healthy';setItems([{name:String(f.get('name')),type:String(f.get('type')),counterparty:String(f.get('counterparty')),expiry,days,status},...items]);setModal(false);setToast('Obligation added successfully')};
return <main className="min-h-screen flex">{toast&&<div className="fixed top-5 right-5 z-50 bg-slate-900 text-white px-4 py-3 rounded-xl shadow-lg text-sm font-semibold">{toast}</div>}<aside className="hidden md:flex w-64 bg-white border-r border-slate-200 p-5 flex-col"><div className="flex items-center gap-2 mb-10"><div className="h-9 w-9 rounded-xl bg-[#174ea6] text-white grid place-items-center font-bold">L</div><span className="text-xl font-bold tracking-tight">LexRenew</span></div><nav className="space-y-1 text-sm"><Link href="/" className="flex gap-3 items-center px-3 py-2.5 rounded-lg bg-slate-100 font-semibold"><LayoutDashboard size={18}/>Dashboard</Link><Link href="/obligations" className="flex gap-3 items-center px-3 py-2.5 rounded-lg text-slate-600 hover:bg-slate-50"><ClipboardList size={18}/>Obligations</Link><Link href="/documents" className="flex gap-3 items-center px-3 py-2.5 rounded-lg text-slate-600 hover:bg-slate-50"><FileText size={18}/>Documents</Link><Link href="/reminders" className="flex gap-3 items-center px-3 py-2.5 rounded-lg text-slate-600 hover:bg-slate-50"><Bell size={18}/>Reminders</Link><Link href="/calendar" className="flex gap-3 items-center px-3 py-2.5 rounded-lg text-slate-600 hover:bg-slate-50"><CalendarDays size={18}/>Calendar</Link><Link href="/team" className="flex gap-3 items-center px-3 py-2.5 rounded-lg text-slate-600 hover:bg-slate-50"><Users size={18}/>Team</Link></nav><div className="mt-auto card p-4 bg-slate-50"><ShieldCheck size={20} className="text-[#174ea6]"/><p className="font-semibold text-sm mt-3">Never miss a renewal.</p><p className="text-xs text-slate-500 mt-1">LexRenew keeps every obligation visible and on time.</p></div></aside><section className="flex-1"><header className="h-16 bg-white border-b border-slate-200 flex items-center justify-between px-5 md:px-8"><div><p className="text-xs text-slate-500">Workspace</p><p className="font-semibold">Acme Industries</p></div><button onClick={()=>setModal(true)} className="bg-[#174ea6] text-white px-4 py-2.5 rounded-lg text-sm font-semibold flex gap-2 items-center"><Plus size={17}/>Add obligation</button></header><div className="max-w-7xl mx-auto p-5 md:p-8"><div className="mb-7"><h1 className="text-2xl md:text-3xl font-bold tracking-tight">Good afternoon</h1><p className="text-slate-500 mt-1">Here’s what needs your attention.</p></div><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-7"><Stat icon={<TriangleAlert size={19}/>} label="Due in 30 days" value={String(items.filter(x=>x.days<=30).length)} tone="danger"/><Stat icon={<Clock3 size={19}/>} label="Upcoming" value={String(items.filter(x=>x.days>30&&x.days<=60).length)} tone="warn"/><Stat icon={<FileText size={19}/>} label="Active obligations" value={String(items.length)} tone="brand"/><Stat icon={<CheckCircle2 size={19}/>} label="On track" value={items.length?`${Math.round(items.filter(x=>x.days>30).length/items.length*100)}%`:'0%'} tone="ok"/></div><div className="mb-6 rounded-2xl border border-blue-100 bg-gradient-to-r from-blue-50 to-white p-5 flex flex-col md:flex-row md:items-center justify-between gap-4"><div className="flex gap-3"><div className="h-10 w-10 rounded-xl bg-white border border-blue-100 grid place-items-center text-[#174ea6]"><Sparkles size={18}/></div><div><p className="font-bold">LexRenew intelligence</p><p className="text-sm text-slate-600 mt-1">{items.length?`Your highest-priority renewal is ${[...items].sort((a,b)=>a.days-b.days)[0].name}, due in ${[...items].sort((a,b)=>a.days-b.days)[0].days} days.`:'Add an obligation to start tracking renewals.'}</p></div></div><Link href="/reminders" className="text-sm font-semibold text-[#174ea6] whitespace-nowrap">Set reminder <ChevronRight size={15} className="inline"/></Link></div><div className="card overflow-hidden"><div className="p-5 border-b border-slate-200 flex flex-col lg:flex-row gap-3 lg:items-center justify-between"><div><h2 className="font-bold">Renewals & obligations</h2><p className="text-sm text-slate-500">Track what is due before it becomes a problem.</p></div><div className="flex gap-2"><div className="relative"><Search size={16} className="absolute left-3 top-3 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search" className="pl-9 pr-3 py-2 border border-slate-300 rounded-lg text-sm w-48 outline-none focus:ring-2 focus:ring-blue-100"/></div><select value={filter} onChange={e=>setFilter(e.target.value)} className="border border-slate-300 rounded-lg px-3 text-sm"><option>All</option><option>Contract</option><option>Lease</option><option>Insurance</option><option>AMC</option><option>Licence</option></select></div></div><div className="divide-y divide-slate-100">{filtered.map((x,i)=><Link href="/obligations" key={i} className="block p-5 flex flex-col md:flex-row md:items-center gap-4 justify-between hover:bg-slate-50"><div className="flex gap-4 items-start"><div className="h-10 w-10 rounded-xl bg-slate-100 grid place-items-center text-slate-600"><FileText size={18}/></div><div><div className="font-semibold">{x.name}</div><div className="text-sm text-slate-500 mt-1">{x.type} · {x.counterparty}</div></div></div><div className="flex items-center gap-5 md:min-w-[340px] md:justify-end"><div className="flex items-center gap-2 text-sm text-slate-600"><CalendarClock size={16}/>{x.expiry}</div><span className={`text-xs font-bold px-2.5 py-1 rounded-full ${x.status==='Urgent'?'bg-red-50 text-red-700':x.status==='Upcoming'?'bg-amber-50 text-amber-700':'bg-emerald-50 text-emerald-700'}`}>{x.days} days</span><ChevronRight size={17} className="text-slate-400"/></div></Link>)}{filtered.length===0&&<div className="p-12 text-center text-slate-500">No obligations found.</div>}</div></div></div></section>{modal&&<div className="fixed inset-0 bg-slate-900/30 grid place-items-center p-5"><form onSubmit={add} className="bg-white rounded-2xl p-6 w-full max-w-md shadow-xl"><h2 className="text-xl font-bold">Add obligation</h2><p className="text-sm text-slate-500 mt-1 mb-5">Create the first record for this renewal cycle.</p><div className="space-y-3"><input name="name" required placeholder="Document / obligation name" className="w-full border rounded-lg px-3 py-2.5"/><select name="type" className="w-full border rounded-lg px-3 py-2.5"><option>Contract</option><option>Licence</option><option>Insurance</option><option>Lease</option><option>AMC</option><option>Certificate</option></select><input name="counterparty" required placeholder="Counterparty / authority" className="w-full border rounded-lg px-3 py-2.5"/><input name="expiry" required type="date" className="w-full border rounded-lg px-3 py-2.5"/></div><div className="flex justify-end gap-2 mt-6"><button type="button" onClick={()=>setModal(false)} className="px-4 py-2.5 rounded-lg border">Cancel</button><button className="px-4 py-2.5 rounded-lg bg-[#174ea6] text-white font-semibold">Save obligation</button></div></form></div>}</main>}
function Stat({icon,label,value,tone}:{icon:React.ReactNode;label:string;value:string;tone:string}){const map:any={danger:'bg-red-50 text-red-700',warn:'bg-amber-50 text-amber-700',brand:'bg-blue-50 text-blue-700',ok:'bg-emerald-50 text-emerald-700'};return <div className="card p-5"><div className={`w-9 h-9 rounded-lg grid place-items-center ${map[tone]}`}>{icon}</div><p className="text-sm text-slate-500 mt-4">{label}</p><p className="text-2xl font-bold mt-1">{value}</p></div>}
